apiVersion: v1
kind: Service
metadata:
  name: roommate-postgres-public
  namespace: {{ .Values.namespace }}
spec:
  type: LoadBalancer
  selector:
    app: roommate-postgres
  ports:
    - port: 5432
      targetPort: 5432